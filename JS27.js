//write a js function without arguments to calculate area of triangle and return its area(print area using document.writeln)
let b=20
let h=40
//let area;
function areaOfTriangle(area)
{
     area=1/2*b*h;
    console.log(`Area of Triangle:${area}`);
    return area;
}
areaOfTriangle();

